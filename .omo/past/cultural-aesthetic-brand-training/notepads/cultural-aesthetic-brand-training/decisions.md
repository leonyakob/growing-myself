# Decisions

## 2026-06-08 Task: session-start
- Work directly in current repository; no worktree path is set in boulder.json.
- Execute Wave 1 Tasks 1-4 in parallel because each writes a separate `.omo/drafts/cabt-*.md` file.
- Use writing/deep categories as specified by the plan; no available skill domain matches plain Markdown curriculum writing, so load_skills=[] is appropriate.
