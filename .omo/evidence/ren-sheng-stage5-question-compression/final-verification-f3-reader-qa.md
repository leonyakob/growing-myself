# F3 Reader QA，`《人生》` Stage 5 读者模拟终检

VERDICT: APPROVE

## 审核范围

- 主审对象：`/home/king/github/growing-myself/.omo/evidence/ren-sheng-stage5-question-compression/task-12-final-output.md`
- 对照材料：`task-10-writing-conversion.md`、`task-11-method-and-crosswork.md`、`task-13-final-qa.md`
- 计划依据：`/home/king/github/growing-myself/.omo/plans/ren-sheng-stage5-question-compression.md:235-239`

## 读者模拟结论

我把 `task-12-final-output.md` 当成用户真正会拿去继续写作的 Stage 5 包来重读，不借助别的内部 evidence 文件做理解跳板。按这个读法，它已经能单独工作。

开头 `3-5` 行先把三条主线说清楚，告诉读者这不是盖棺定论，而是要把阅读和写作先收紧到“安身位置，外部目光，关系责任”三根线上。接着 `7-41` 行把三个核心问题逐个立住，每题都先给用户原句引信，再给分析压缩，读者能立刻知道自己接下来到底在追哪一个问题，不需要再回头翻 QA 文件或中间稿。

更关键的是，这份终稿不只是“结构完整”，而是真的能指导动笔。`137-161` 行把每个核心问题都拆成 `短札`、`主题文章`、`长文 / 系列` 三层写法，每一层都给了标题、子问题、先抓哪一幕、最容易写坏什么。读者如果今天只想写一则短札，可以直接从问题二的 `一个副词，已经开始撤退了` 下手；如果想写主题文章，可以直接走问题一的 `县城先像启蒙，后来又像跳板`；如果想扩成长文，可以直接顺着问题三的 `从保密到“下不了台”，爱情怎样被一步步改写成巧珍的代价` 往下写。这里的指导已经具体到可开笔，不是泛泛告诉读者“你可以展开”。

## 逐条要求核验

### 1. 能否不打开内部 evidence 文件，直接继续写作

结论：能。

理由：

- 三问本身已经把写作中心压出来了，`9-41` 行不是抽象主题词，而是可直接转写的问句。
- `43-77` 行给出每题的证据和反证，能帮助读者避免写扁。
- `79-95` 行张力地图告诉读者每题最值得抓的对撞面。
- `97-135` 行五异法细读点给出词、动作、沉默、结构转轴这些细部入口。
- `137-161` 行写作转化把短札、主题文章、长文路径都已经铺开。

单看这一个文件，读者已经能完成“选题，选场景，定篇幅，避坑，决定哪些短语需要回书复核”这五步。

### 2. 是否是清楚的 reader-facing Stage 5 包

结论：是。

它的章节顺序符合读者使用顺序，不是按内部流程编号排的。先是三问，再是证据表、张力图、五异法、写作转化、写法档案、跨作品联动、待原文复核清单、自检与可靠性说明。这个顺序和 `learnings.md:44-47` 里总结的最佳 reader-facing 打包顺序一致，也确实读起来顺手。

### 3. 三个核心问题和写作支路是否分得清楚

结论：分得清楚。

- 核心问题集中放在 `## 三个核心问题` 下，只出现 `### 问题一`、`### 问题二`、`### 问题三` 三个主标题。
- 写作分叉没有伪装成新核心题，而是统一放在 `## 写作转化` 下面，用 `### 问题一的写作支路`、`### 问题二的写作支路`、`### 问题三的写作支路` 承接。
- 每个支路表都把 `短札`、`主题文章`、`长文 / 系列` 明确列为层级，不会让读者误以为又冒出了第四问或第五问。

### 4. `自检与可靠性说明` 是否自然

结论：自然。

`217-246` 行不是 workflow 话术，而是在用读者能懂的话解释三件事，这份整理最稳的地方是什么，写的时候最容易错在哪里，还有它的可靠性边界在哪里。尤其 `244-246` 行把“哪些地方可先当阅读锚点，哪些地方若要上正式引文还得回书”讲得很自然，没有把读者拉进 `.omo`、ledger、task 状态这类内部术语里。

### 5. 有没有泄漏 `.omo` mechanics 或技术 ID

结论：没有。

Reader-facing 终稿里没有 `.omo`、`source ID`、`range`、`chapterUid`、`bookId`、`ledger`、`snapshot`、`task`、`wave`、`QA state` 这类内部词，也没有内部编号式口吻。

### 6. `待原文复核` 是否用自然读者语言解释

结论：是。

`189-215` 行先说“这些短语已经能作为读书卡片里的锚点使用，但如果你要把它们当小说逐字引文放进正式文章，还得再回原书核对一遍”，后面又补了“场景可以先用，逐字引文要后核”“卡片里的短语能先当阅读路标，不能直接冒充定本原文”。这是读者语言，不是流程黑话。

### 7. 写作指导是否真的可用

结论：可用。

我特意按一个真实读者的使用路径试走了一遍：先选问题，再选篇幅，再看证据和避坑。这个流程在 `task-12-final-output.md` 里已经闭环，不需要额外打开 `task-10` 或 `task-11` 才能补全“怎么写”。`task-10` 和 `task-11` 更像来源支撑，而不是读者使用时的必需拐杖。

## 机械检查

### readback 结论

- 已全文读回 `task-12-final-output.md`。
- 已对照读 `task-10-writing-conversion.md`、`task-11-method-and-crosswork.md`、`task-13-final-qa.md`，确认终稿确实把前两份的写作转化和写法/联动内容吸收进了一个 reader-facing 包，而不是要求读者自己去拼接来源文件。

### grep 检查

1. `grep("\\.omo|source ID|range|chapterUid|bookId|ledger|snapshot|task|wave|QA state")`
   - 结果：`0` 命中。
2. `grep("问题四|问题五|第四个核心问题|第五个核心问题|FQ-04|FQ-05|hidden fourth|hidden fifth")`
   - 结果：`0` 命中。
3. `grep("TODO|FIXME|HACK|TBD|待补|占位|placeholder|xxx")`
   - 结果：`0` 命中。

### Python 结构检查

- `CORE_HEADINGS 3`
- `EXTRA_CORE_HEADINGS 0`
- `MISSING_SECTIONS NONE`
- `问题一支路 1`
- `问题二支路 1`
- `问题三支路 1`
- `短札 3`
- `主题文章 3`
- `长文/系列 3`
- `FORBIDDEN_MATCHES 0`
- `REVIEW_MARKER_COUNT 33`
- `reader_explainer_sentence PASS`
- `principle_scene_before_quote PASS`
- `principle_anchor_not_quote PASS`

这些结果说明，终稿里恰好只有三道核心问题，九个 reader-facing 主分区都在，三道题都各自带着三层写作分叉，而且 `待原文复核` 不只是标签，还带着读者能懂的说明句。

### scope baseline，写入本证据文件之前

已运行 `git status --short --untracked-files=all`。

结果显示仓库本来就是脏工作区，主要集中在 `.omo` runtime 状态、plan、evidence、notepads 和 continuation JSON 路径。写入本文件之前，未见 `/home/king/github/growing-myself/路遥/人生/《人生》阅读笔记.md`、提示词文件或 `AGENTS.md` 出现在状态输出中。

### 创建后复核

1. readback
   - 已读回本文件，确认 `3` 行存在清晰 verdict，内容为 `VERDICT: APPROVE`。
   - 已确认本文件把读者模拟结论，逐条核验，grep 结果，Python 结果，scope 记录都写在同一处，没有把关键判断散落到别的文件里。

2. `lsp_diagnostics`
   - 结果：`No LSP server configured for extension: .md`。
   - 处理：按本轮既定口径记为工具不可用，不算失败。本次改用 readback、grep、Python 和 `git status` 做确定性核验。

3. 写入本文件之后的 `git status --short --untracked-files=all`
   - 结果中新增了本文件 `?? .omo/evidence/ren-sheng-stage5-question-compression/final-verification-f3-reader-qa.md`。
   - 同时还能看到并行波次里的 `final-verification-f1-plan-compliance.md`，但它不是本次 F3 写入对象。
   - 仍未见 `/home/king/github/growing-myself/路遥/人生/《人生》阅读笔记.md`、提示词文件、`AGENTS.md`、stage/commit/push 相关痕迹。

## 当前判定

本次 F3 不是只看格式，而是按“读者拿到这份包，能不能直接继续写”来判。我的结论是，可以直接继续写，因此给出 `VERDICT: APPROVE`。
